<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosa.todoup.mapper.DiaryMapper">

    <insert id="insertDiary" parameterType="com.kosa.todoup.dto.DiaryDTO">
        INSERT INTO
            diary(user_id, diary_date, emotion, weather, content, img_url)
        VALUES
            (#{userId}, #{diaryDate}, #{emotion}, #{weather}, #{content} #{imgUrl})
    </insert>

</mapper>
